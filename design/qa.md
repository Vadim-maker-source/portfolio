prototype: http://localhost:3000  
source: `design/context.md` + концепция «Инженерная сцена» + аудит скриншота

## Визуальное соответствие

- ✓ Реальная фотография занимает всю площадь hero через `fill` и `object-cover`.
- ✓ Изображение принудительно монохромное; бирюзовый и синий исходного интерфейса не добавлялись.
- ✓ Использованы только плоские чёрные слои. Нет градиентов, blur, glow или цветного освещения.
- ✓ Смысловой блок находится в средней части viewport, а не прижат к нижней границе.
- ✓ Главный текст сообщает позиционирование: `Собираю цифровые системы целиком.`
- ✓ Никнейм уменьшен до авторской подписи и не дублируется на фоне.
- ✓ Нижняя зона представляет собой одну строку направлений без таблицы, карточек и вертикальных ячеек.
- ✓ Фиолетовый используется только в смысловом слове `целиком`, основном CTA и интерактивных состояниях.

## Поведение

- ✓ Единственная постоянная анимация страницы остаётся в ранее запрошенном следе курсора.
- ✓ Основной CTA ведёт к существующему `#experience`.
- ✓ Контакт ведёт к существующему `#contact`.
- ✓ GitHub и Telegram открываются безопасно в новой вкладке.
- ✓ Hover не поднимает кнопки или блоки; меняется только цвет.

## Accessibility sanity

- ✓ Фотография декоративная и имеет пустой `alt`.
- ✓ Один семантический `h1`, отдельный `nav`, список направлений размечен через `ul`.
- ✓ Ссылки имеют минимальную высоту 48px и видимое focus-состояние.
- ✓ Внешние ссылки снабжены уточняющими `aria-label`.
- manual: точный контраст текста на всех участках фотографии требует визуальной browser-проверки.
- manual: полный screen-reader pass не проводился.

## Техническая проверка

- ✓ ESLint проходит.
- ✓ Production build проходит.
- ✓ Страница отвечает HTTP 200.
- ✓ Локальный JPG отвечает HTTP 200, размер 893190 байт.
- ✓ Новый заголовок присутствует в серверном HTML.
- manual: browser-capture в текущей сессии недоступен, поэтому пиксельная оценка desktop/mobile по готовой странице не заявляется.

verdict: **READY на уровне кода и требований.** Блокирующих расхождений с концепцией нет; окончательный визуальный вердикт следует дать по свежему скриншоту готового hero.
